# dependencies
node_modules/

# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/

# 作業中の一時物・確認用スクショ(リポジトリに混ぜない)
.playwright-mcp/
*.png
*.log

# Cowork の出力先設定が直るまでの保険(実体は ../_scratch/ へ退避する)
Claude outputs/

# secrets(このリポジトリは静的UIライブラリで秘密は持たないが念のため)
.env
.clasprc.json
*.serviceaccount.json
